@AGENTS.md

Read user-wide `AGENTS.md` first. It is the canonical portable workflow.

Use `KICKOFFS.md` only when the user asks to plan, execute a scoped work item, review a
PR/diff, or patch reviewer findings.

Repo/subtree `AGENTS.md` files override this kernel for stack commands, file ownership,
and local hazards.
